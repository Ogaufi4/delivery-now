import 'package:flutter/material.dart';

// === BRAND COLORS ===
const Color primaryColor = Color(0xFF00A8B5); // New brand teal
const Color errorColor = Color(0xFFFF4D4D);
const Color secondaryColor = Color(0xFF00A79F); // Slightly darker teal
const Color onPrimaryColor = Color(0xFF00A8B5); // White text on primary
const Color onSecondaryColor = Color(0xFF00A8B5); // White text on secondary

// === BACKGROUND & SURFACE COLORS ===
const Color backgroundColor = Color(0xFFF7F7F7); // Light gray background (keep as is)
const Color splasBackgroundColor = Color(0xFF292929); // Dark gray (keep as is)
const Color onBackgroundColor = Color(0xFFFFFFFF); // White text on light backgrounds
const Color textFieldBackground = Color(0xFFf9f9f9); // Light input background (keep)
const Color white = Color(0xFFFFFFFF);
const Color timeLineColor = Color(0xffb6b6b6);

// === TEXT & UI ELEMENTS ===
const Color black = Color(0xFF000000);
const Color lightFont = Color(0xFFa5a5a5);
const Color lightFontColor = Color(0xFF939495);
const Color greayLightColor = Color(0xFF4f4f4f);

// === BORDERS & SHADOWS ===
const Color textFieldBorder = Color(0xFFacacac);
const Color commentBoxBorderColor = Color(0xFFd7d7d7);
const Color shadow = Color(0x0f000000);
const Color shadowBottomBar = Color(0x1a2c272e);
const Color shadowCard = Color(0x29000000);
const Color shaderColor = Color(0x00808080);

// === ORDER TRACKING CARD COLORS (Updated to use brand colors) ===
const Color orderTrackingCardYellowColor = Color(0xFFEAB308); // Use a warm yellow
const Color orderTrackingCardRedColor = Color(0xFFFF4D4D); // Keep red for error states
const Color orderTrackingCardGreenColor = Color(0xFF33BC88); // Keep green for success
const Color orderTrackingCardBlueColor = Color(0xFFE6E6F6); // Soft blue for info
const Color orderTrackingCardOrangeColor = Color(0xFF00C7E7); // Bright cyan/orange alternative
const Color orderTrackingCardPeachColor = Color(0xFFFFD199); // Peach tone for neutral status

// === SOCIAL MEDIA COLOR ===
const Color facebookColor = Color(0xFF4267B2);

// === OTHER BRAND COLORS ===
const Color yellowColor = Color(0xFFEBAC17);
const Color blueColor = Color(0xFF0DCAF0);

// === SHIMMER LOADING COLORS ===
Color shimmerBaseColor = Colors.grey.shade300;
Color shimmerhighlightColor = Colors.grey.shade100;
Color shimmerContentColor = Colors.white.withAlpha(210); // Alpha 0.85 = 210


// === BUTTON TEXT COLOR ===
const Color buttonTextColor = Color(0xFF292929); // Dark text for buttons
